[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn stockpred.backend.api:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
